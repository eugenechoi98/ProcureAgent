[project]
name = "procureguard"
version = "0.1.0"
description = "ProcureGuard AI shared contract layer"
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2.0,<3.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
